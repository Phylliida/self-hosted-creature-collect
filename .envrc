# Enable flakes for this project only — scoped to whatever direnv
# loads, doesn't touch your global Nix config. shell.nix uses
# `builtins.getFlake` to pull in iloader, which requires the
# `nix-command` and `flakes` experimental features.
export NIX_CONFIG="extra-experimental-features = nix-command flakes"

use nix
